targetNamespace: portworx

subscription:
  channel: stable
  installPlanApproval: automatic

portworx:
  is_default_storage_class: true
  nfs_server: nfs-server
  nfs_policy: export-policy

routingViaHost:
  enabled: true
  image: registry.redhat.io/openshift4/ose-cli:latest
