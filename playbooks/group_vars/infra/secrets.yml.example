---
# Copy this file to secrets.yml (gitignored) in this same directory and fill
# in real values. secrets.yml is auto-loaded by Ansible alongside vars.yml
# whenever cluster_name=infra is used.
pull_secret: '<pull secret to download from Red Hat>'
ssh_key: '<contents of id_rsa_ocp.pub>'
